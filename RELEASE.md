# คู่มือการอัปโค้ดขึ้น GitHub และออกเวอร์ชันใหม่ (สำหรับผู้ดูแลระบบ)

เอกสารนี้สำหรับ **ผู้พัฒนา/ผู้ดูแล** ที่แก้โค้ดแล้วต้องการเผยแพร่ขึ้น GitHub เพื่อให้หน่วยบริการทั้งหมดดึง (pull) ไปใช้
(ถ้าคุณเป็นหน่วยบริการที่จะ **ติดตั้งไปใช้งาน** ให้ดู [INSTALL.md](INSTALL.md) แทน)

> **แนวคิดสำคัญ:** หน่วยบริการดึงโค้ดตาม **git tag** (เช่น `v1.0.0`, `v1.1.0`) ไม่ใช่ branch `master`
> ดังนั้นทุกครั้งที่จะปล่อยของใหม่ ต้อง (1) commit → (2) ตั้ง tag เวอร์ชันใหม่ → (3) push ทั้ง commit และ tag
>
> **แพ็กเกจอยู่บน Google Drive ไม่ใช่ GitHub แล้ว** (เปลี่ยนเมื่อ 6 ส.ค. 2569)
> หน่วยบริการอ่านรายการเวอร์ชันจากโฟลเดอร์ **Version Control** บน Drive ของผู้ดูแล
> ทั้งปุ่มอัปเดตในหน้าเว็บและเมนู 1 ของตัวช่วยติดตั้ง — **git tag ยังตั้งเหมือนเดิม**
> เพื่อทำเครื่องหมายในประวัติโค้ด แต่ตัว tag ไม่ใช่สิ่งที่หน่วยบริการดึงไปใช้อีกต่อไป
>
> เหตุผลที่ย้าย: แพ็กเกจเคยถูกสร้างโดย GitHub Actions วันที่ 6 ส.ค. 2569 Actions ล่มยาว
> (major outage) ไฟล์ของเวอร์ชันใหม่จึงไม่เคยถูกสร้าง ปล่อยเวอร์ชันไม่ได้เลยทั้งวัน
> อีกเรื่องคือ GitHub API จำกัด 60 ครั้ง/ชม. **ต่อ IP** ซึ่งทุกเครื่องในหน่วยเดียวกันแชร์กัน
>
> **ขั้นตอนปล่อยเวอร์ชัน:** build เอง แล้วอัปไฟล์ขึ้นโฟลเดอร์นั้น (ดูหัวข้อ "สร้างแพ็กเกจ
> และอัปขึ้น Drive" ด้านล่าง) ไม่ต้องรอระบบอัตโนมัติของใครทั้งนั้น
>
> **ชื่อไฟล์ต้องมีเลขเวอร์ชัน** เช่น `ndp-kit-6908060553.zip` เพราะระบบอ่านเลขเวอร์ชัน
> จากชื่อไฟล์โดยตรง ไฟล์ที่ชื่อไม่มีเลขสิบหลักจะถูกข้ามไปเงียบๆ (ตั้งใจให้เป็นแบบนั้น
> เพื่อให้วางไฟล์อื่นปนในโฟลเดอร์ได้) เลขเวอร์ชันจริงยังต้องอยู่ใน `version.txt`
> ข้างใน zip ด้วย เพราะตัวอัปเดตเทียบสองค่านี้กันเพื่อกันไฟล์ผิดตัว
>
> **เวอร์ชัน Node ต้องตรงกันสองที่:** `node-version` ใน `release.yml` กับ `$NODE_VER` ใน
> `install/ndp-kit-setup.ps1` (ปัจจุบันคือ 24 / `v24.19.0`) เพื่อให้เวอร์ชันตอน build
> กับตอนที่หน่วยบริการใช้รันเป็นตัวเดียวกัน
>
> **การอัปเดตในหน้าเว็บเปลี่ยนแค่โฟลเดอร์ `app/` ไม่เคยเปลี่ยน `node/`** ดังนั้นถ้ารุ่นใหม่
> ต้องการ Node สูงกว่าที่เครื่องหน่วยบริการมีอยู่ (เช่นอัป Next ข้าม major) เครื่องเหล่านั้น
> จะอัปเดตแล้วเปิดโปรแกรมไม่ขึ้น และย้อนกลับเองไม่ได้เพราะไฟล์เก่าถูกลบไปแล้ว
> ก่อนปล่อยรุ่นที่ขยับข้อกำหนด Node ต้องเช็คก่อนว่า `$NODE_VER` ที่ทุกเครื่องติดตั้งไว้สูงพอ
> ถ้าไม่พอ ต้องให้หน่วยบริการรันตัวช่วยติดตั้งเมนู `[1]` ใหม่ (ซึ่งลง Node ใหม่ให้ด้วย)
> แทนการกดอัปเดตในหน้าเว็บ

---

## 0. เตรียมครั้งแรกครั้งเดียว

ตรวจว่ามี remote ชี้ไป GitHub แล้ว:

```bash
git remote -v
# ควรเห็น: origin  https://github.com/tankitkitty/ndp-kit.git
```

ถ้ายังไม่มี ให้เพิ่ม:

```bash
git remote add origin https://github.com/tankitkitty/ndp-kit.git
```

**เรื่อง login GitHub:** ตอน push ครั้งแรก GitHub จะให้ยืนยันตัวตน
- **วิธีง่ายสุด (Windows):** ติดตั้ง [Git for Windows](https://git-scm.com/) ซึ่งมาพร้อม Git Credential Manager — ครั้งแรกจะเด้งหน้าต่างให้ล็อกอิน GitHub ผ่านเบราว์เซอร์ แล้วจำให้อัตโนมัติ
- **หรือใช้ Personal Access Token (PAT):** สร้างที่ GitHub → Settings → Developer settings → Personal access tokens → เลือกสิทธิ์ `repo` แล้วใช้ token นั้นแทนรหัสผ่านตอน push

---

## 1. เช็คลิสต์ก่อนปล่อยเวอร์ชัน (สำคัญมาก)

ทำทุกข้อก่อน commit เสมอ:

```bash
# 1) ตรวจ type ผ่าน
npx tsc --noEmit

# 2) build ผ่าน (จำลองสภาพจริงที่หน่วยบริการจะรัน)
npm run build

# 3) ตรวจว่าไม่มีไฟล์ลับหลุดเข้า git — ต้องไม่เห็นไฟล์เหล่านี้ในผลลัพธ์
git status --short | grep -E 'dbconfig|session-secret|\.env'
#   ถ้าไม่มี output = ปลอดภัย ✓  ถ้ามี = อย่า commit เด็ดขาด
```

ไฟล์ที่ **ห้าม** ขึ้น git (ถูกกันไว้ใน `.gitignore` แล้ว แต่ควรตรวจซ้ำ):
`data/dbconfig.json`, `data/dbconfig43.json`, `data/.session-secret`, `.env.local`, `node_modules/`

---

## 2. ขั้นตอนออกเวอร์ชันใหม่

สมมติเวอร์ชันปัจจุบันคือ `v1.0.0` และเราจะออก `v1.1.0`

```bash
# 1) ดูว่ามีอะไรเปลี่ยนบ้าง
git status

# 2) เพิ่มไฟล์ที่แก้ทั้งหมดเข้า staging
git add -A

# 3) ตรวจซ้ำอีกครั้งว่าไม่มีไฟล์ลับ
git diff --cached --name-only | grep -E 'dbconfig|session-secret|\.env' || echo "ปลอดภัย"

# 4) commit พร้อมข้อความอธิบายว่าทำอะไร
git commit -m "อธิบายสั้นๆ ว่าเวอร์ชันนี้แก้/เพิ่มอะไร"

# 5) ตั้ง tag เวอร์ชันใหม่ (annotated tag)
git tag -a v1.1.0 -m "NDP-Kit v1.1.0 - สรุปสิ่งที่เปลี่ยน"

# 6) push ทั้ง commit และ tag ขึ้น GitHub
git push origin master
git push origin v1.1.0
```

> ย่อขั้นตอน 6 ให้เหลือคำสั่งเดียว (push commit + tag ทั้งหมดพร้อมกัน):
> ```bash
> git push origin master --tags
> ```

### 2.1 สร้างแพ็กเกจและอัปขึ้น Drive (ขั้นที่ทำให้หน่วยบริการเห็นเวอร์ชันใหม่จริงๆ)

push tag อย่างเดียว **หน่วยบริการยังไม่เห็นอะไรทั้งนั้น** ต้องสร้างไฟล์แล้วอัปขึ้น
โฟลเดอร์ Version Control บน Google Drive ด้วย

รันคำสั่งเดียว (คำนวณเลขเวอร์ชัน → ตรวจโค้ด → build → ประกอบ → บีบอัด → ตรวจไฟล์)

```powershell
.\scripts\release.ps1
```

| ตัวเลือก | ใช้ทำอะไร |
| --- | --- |
| `-Version 6908070601` | กำหนดเลขเวอร์ชันเอง แทนการคำนวณจากเวลาปัจจุบัน |

สคริปต์จะหยุดทันทีถ้า `tsc` ไม่ผ่าน, build ไม่ผ่าน, เจอไฟล์ลับในแพ็กเกจ,
หรือ `version.txt` ไม่ตรงกับเลขในชื่อไฟล์

**ขั้นสุดท้ายคือลากไฟล์เข้า Drive เอง** สคริปต์จะบอกที่อยู่ไฟล์ให้ พร้อมย้ำว่า
ยังไม่ได้ปล่อยเวอร์ชันจนกว่าไฟล์จะขึ้นไปจริง

> **ทำไมไม่อัปให้อัตโนมัติ**
>
> เคยลองใช้ rclone แล้วเลิก (7 ส.ค. 2569) เพราะต้องเก็บ token ที่เข้าถึง Google Drive
> ได้ทั้งบัญชีไว้ในเครื่อง แลกกับการประหยัดแรงลากไฟล์ครั้งละสิบวินาที ไม่คุ้มกัน
> อีกข้อคือ rclone ใช้ client_id กลางที่ Google ประกาศจะเลิกรองรับภายในปี 2569
> ถ้าไปพึ่งไว้ วันที่มันหยุดทำงานการปล่อยเวอร์ชันจะพังโดยไม่เกี่ยวกับโค้ดเราเลย
>
> ปล่อยเวอร์ชันไม่กี่ครั้งต่อสัปดาห์ ลากไฟล์เองจึงเป็นการแลกที่ถูกกว่า

### ตรวจก่อนแจ้งหน่วยบริการ

ลากไฟล์ `ndp-kit-<เลขเวอร์ชัน>.zip` เข้าโฟลเดอร์ Version Control บน Drive
แล้วตรวจ 3 อย่าง

1. การแชร์ของโฟลเดอร์ยังเป็น **"ทุกคนที่มีลิงก์ • ผู้อ่าน"**
2. ชื่อไฟล์มีเลขสิบหลัก และลงท้าย `.zip` (สคริปต์ตั้งชื่อให้ถูกอยู่แล้ว ห้ามเปลี่ยน)
3. ไฟล์ขึ้นครบ 100% ไม่ใช่ค้างอยู่ระหว่างอัป

> **ห้ามบีบอัดเองด้วย "Send to > Compressed folder" ของ Windows หรือ PowerShell**
> `Compress-Archive` และ `ZipFile::CreateFromDirectory` บน .NET Framework เขียนชื่อ
> ไฟล์ข้างในด้วย `\` ซึ่งผิดสเปก ZIP ตัวแตกไฟล์ฝั่งหน่วยบริการจะได้ไฟล์ชื่อ
> `.next\BUILD_ID` แบนๆ อยู่โฟลเดอร์เดียวกันหมด แล้วโปรแกรมจะเปิดไม่ขึ้นทั้งหน่วย
> ให้ใช้ `scripts/release.ps1` หรือ `node scripts/pack.js` เท่านั้น (เจอจริงแล้ว 7 ส.ค. 2569)

> **เวอร์ชันเก่าอย่าเพิ่งลบ** เก็บไว้สัก 2-3 ตัว เพราะหน้าเว็บและตัวช่วยติดตั้งให้
> เลือกติดตั้งย้อนกลับได้ ถ้าเวอร์ชันใหม่มีปัญหาที่หน่วยบริการจะได้ถอยเองได้ทันที
> ไม่ต้องรอผู้ดูแลปล่อยตัวแก้

---

## 3. push เวอร์ชันแรก (v1.0.0) ที่ commit ไว้แล้ว

ตอนนี้ในเครื่องมี commit และ tag `v1.0.0` อยู่แล้ว แต่ยังไม่ได้ push ขึ้น GitHub
ปล่อยเวอร์ชันแรกด้วยคำสั่งเดียว:

```bash
git push origin master --tags
```

หลัง push แล้ว เข้าไปดูที่ `https://github.com/tankitkitty/ndp-kit/tags` ควรเห็น `v1.0.0`

---

## 4. วิธีตั้งหมายเลขเวอร์ชัน (เลขวันเวลา 10 หลัก)

เลิกใช้ `vMAJOR.MINOR.PATCH` แล้วตั้งแต่ 6 ส.ค. 2569 เวอร์ชันคือ **เวลาที่สร้างแพ็กเกจ**
เป็นเลขสิบหลักติดกัน `ปปดดววชชนน`

```
6 9 0 8 0 6 0 5 5 3   =   6 ส.ค. 69 เวลา 05:53 น.
└┬┘ └┬┘ └┬┘ └┬┘ └┬┘
 │   │   │   │   └── นาที
 │   │   │   └────── ชั่วโมง (แบบ 24 ชม.)
 │   │   └────────── วันที่
 │   └────────────── เดือน
 └────────────────── ปี พ.ศ. สองหลักท้าย (69 = 2569)
```

เทียบกันด้วยการเทียบตัวเลขตรงๆ **เลขมากกว่า = ใหม่กว่า** เพราะเรียงจากหน่วยใหญ่ไปเล็ก

ข้อดีคือไม่ต้องตัดสินใจว่าการแก้ครั้งนี้ควรขยับหลักไหน (ซึ่งเถียงกันได้ไม่จบและไม่ได้
ช่วยอะไรกับงานนี้เลย) และเห็นทันทีว่าเครื่องหนึ่งใช้ของที่สร้างเมื่อไร ซึ่งเป็นสิ่งที่
ต้องรู้จริงๆ เวลาไล่ปัญหาที่หน่วยบริการทางโทรศัพท์

หาเลขของตอนนี้ได้ด้วย:

```bash
node -e "const d=new Date(),p=n=>String(n).padStart(2,'0');console.log(String(d.getFullYear()+543).slice(-2)+p(d.getMonth()+1)+p(d.getDate())+p(d.getHours())+p(d.getMinutes()))"
```

> **เลขแบบเก่ายังใช้ได้อยู่** เครื่องที่ติดตั้ง `v2.0.26` ไว้ยังเทียบเวอร์ชันได้ถูก
> กฎคือเลขแบบเวลาถือว่าใหม่กว่าแบบ `x.y.z` เสมอ ซึ่งตรงกับความจริงเพราะทุกแพ็กเกจ
> ที่ใช้เลขแบบเวลาถูกสร้างหลังจากเลิกใช้ `x.y.z` ไปแล้ว (ดู `lib/version.ts`)

---

## 5. หน่วยบริการอัปเดตยังไง (แจ้งให้ผู้ใช้ทำ)

เมื่อคุณ push tag ใหม่และ workflow แนบ `ndp-kit.zip` เรียบร้อยแล้ว แจ้งหน่วยบริการแค่ว่า

> รันตัวช่วยติดตั้ง (`ndp-kit-setup.bat`) แล้วเลือกเมนู **`[1]`** อีกครั้ง

เท่านั้น ตัวช่วยจะดึงเวอร์ชันล่าสุดมาแทนที่ให้เอง ไม่ต้องใช้ git หรือ npm บนเครื่องหน่วยบริการเลย

> ค่าตั้งค่าของแต่ละหน่วย (`data/dbconfig.json`, `data/dbconfig43.json`, `data/.session-secret`)
> ถูกยกออกมาพักไว้ก่อนวางไฟล์ใหม่แล้วคืนกลับ จึง **ไม่หาย** เวลาอัปเดต — ไม่ต้องตั้งค่าใหม่
>
> **ตรวจก่อนแจ้งทุกครั้ง:** เปิดโฟลเดอร์ Version Control บน Drive ดูว่าไฟล์ของเวอร์ชันใหม่
> อัปขึ้นครบและชื่อมีเลขเวอร์ชันจริง การ push tag อย่างเดียวไม่ทำให้ใครเห็นเวอร์ชันใหม่
> (ดูขั้นตอนที่ 2.1) ถ้าลืมอัปไฟล์ หน่วยบริการจะยังเห็นแต่เวอร์ชันเดิมโดยไม่มีอะไรฟ้อง

---

## 6. ปัญหาที่พบบ่อยตอน push

| อาการ | วิธีแก้ |
| --- | --- |
| `Authentication failed` | ใช้ Personal Access Token แทนรหัสผ่าน (ดูข้อ 0) หรือปล่อยให้ Git Credential Manager เด้งหน้าล็อกอิน |
| `Updates were rejected (fetch first)` | มีคนอื่น/เครื่องอื่น push ไปก่อน — รัน `git pull --rebase origin master` แล้วค่อย push ใหม่ |
| เผลอ commit ไฟล์ลับ (dbconfig/.env) ไปแล้วแต่ยังไม่ push | `git rm --cached <ไฟล์>` แล้ว commit ใหม่ ตรวจว่าไฟล์อยู่ใน `.gitignore` |
| เผลอ push ไฟล์ลับขึ้น GitHub แล้ว | ถือว่ารหัสผ่านนั้น**หลุดแล้ว** ต้องเปลี่ยนรหัสผ่าน DB ทันที และลบไฟล์ออกจากประวัติ git (ปรึกษาก่อนทำ เพราะซับซ้อน) |
| push tag แล้ว Actions ไม่ทำงาน (ไม่มี run เกิดขึ้น) | เกิดตอนที่ commit นั้น**เพิ่งเพิ่มไฟล์ workflow เข้ามาพร้อมกับ tag** — GitHub ยังไม่รู้จัก workflow ตอนประมวลผล event ของ tag แก้โดย push tag ซ้ำ: `git push origin :refs/tags/v1.1.0` แล้ว `git push origin v1.1.0` (tag ยังชี้ commit เดิม ไม่มีอะไรหาย) — release ครั้งถัดๆ ไปไม่เจอปัญหานี้ |
