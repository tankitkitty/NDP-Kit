import { useEffect } from "react";

/**
 * เรียก /api/register-hospital เงียบๆ ตอนเปิดหน้าหลัก ไม่แสดงผลอะไรบนจอ
 *
 * ฝั่งเซิร์ฟเวอร์เป็นคนตัดสินใจทั้งหมดว่าจะทำอะไร: ถ้าหน่วยบริการเคยยินยอมไว้แล้ว
 * และเพิ่งอัปเดตเป็นเวอร์ชันใหม่ จะส่งข้อมูลชุดเดิมซ้ำเพื่อให้คอลัมน์เวอร์ชันในชีต
 * ตรงกับความจริง ถ้าเคยปฏิเสธหรือเวอร์ชันยังเท่าเดิมก็ไม่ทำอะไรเลย
 *
 * ต้องอยู่หน้าหลักเพราะเป็นหน้าที่ทุกคนเข้าหลังล็อกอิน ถ้าไปวางไว้แต่ในหน้าตั้งค่า
 * หน่วยที่อัปเดตแล้วไม่เคยเปิดหน้านั้นอีกจะไม่มีวันรายงานเวอร์ชันใหม่
 */
export default function RegistrySync() {
  useEffect(() => {
    fetch("/api/register-hospital").catch(() => undefined);
  }, []);

  return null;
}
