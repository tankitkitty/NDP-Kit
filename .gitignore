node_modules
.next
# โฟลเดอร์ build ชั่วคราวตอนตรวจว่าโค้ดผ่าน โดยไม่ไปล้าง cache ของ next dev (ดู next.config.js)
.next-verify
out
tsconfig.tsbuildinfo
.DS_Store
.env.local
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# ไฟล์ตั้งค่า/ความลับของแต่ละหน่วยบริการ — เขียนแบบไม่ผูกกับรากโปรเจกต์
# เพื่อให้ครอบ data/ ที่ตัวช่วยติดตั้งสร้างไว้ในโฟลเดอร์ย่อยด้วย (เช่น install/data/)
dbconfig.json
dbconfig43.json
.session-secret
.setup-token
.setup-done
.registered

# ไฟล์ที่ตัวช่วยติดตั้ง/ผู้ดูแลวางไว้ข้างสคริปต์สำหรับติดตั้งแบบออฟไลน์
# รวมถึงซอร์สโค้ดที่เผลอโหลดมาไว้ตรงนี้ — ไม่ควรขึ้น git เพราะซ้ำกับตัว repo เอง
install/*.zip
