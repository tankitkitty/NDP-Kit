node_modules
.next
out
tsconfig.tsbuildinfo
.DS_Store
.env.local
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# ไฟล์ตั้งค่า/ความลับของแต่ละหน่วยบริการ — เขียนแบบไม่ผูกกับรากโปรเจกต์
# เพื่อให้ครอบ data/ ที่ตัวช่วยติดตั้งสร้างไว้ในโฟลเดอร์ย่อยด้วย (เช่น install/data/)
dbconfig.json
dbconfig43.json
.session-secret

# ไฟล์ที่ตัวช่วยติดตั้งสร้างเองตอนรัน ไม่ใช่ซอร์ส
install/docker-compose.yml
